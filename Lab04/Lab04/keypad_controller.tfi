


// Instantiate the module
keypad_controller instance_name (
    .reset(reset), 
    .clk(clk), 
    .column(column), 
    .row(row), 
    .interrupt(interrupt), 
    .keypad_data(keypad_data)
    );


